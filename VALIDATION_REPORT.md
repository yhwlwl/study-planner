# Study Planner v0.8.0 完成性与验证报告

## 一、结论

v0.8.0 已基于完整 v0.7.0 源代码实施，而不是基于最初误传的补丁包重建。最终冻结需求中属于 v0.8 范围的架构、数据模型、界面入口、调度、可解释方案、复盘、自适应时长、计划版本、统计、游客/账号边界、移动端和性能保护均已落入代码。

当前自动验证结果为 **44 / 44 通过**。严格 TypeScript 检查通过；规定规模夹具通过。

发布完成性分为两层：

- **代码实施完成度：100%（按 v0.8 范围与追踪表）**。
- **当前环境可执行验收完成度：约 92%**。尚缺真实 npm 依赖生产构建、真实 iPhone PWA 设备验收和真实 Supabase 项目端到端验收。这三项是环境验证缺口，不是用虚假“通过”替代的代码结论。

因此本次交付判定为：**功能与架构实施完成，发布前外部环境验收待执行**。

## 二、自动验证

执行命令：

```bash
npm run validate:v08
```

结果：

- 版本、迁移、模型、创建、目标、调度、方案、复盘、版本、云端、游客、移动端和 AI 边界等 44 项检查全部通过。
- `npm run typecheck` 通过。
- 详细结果：`validation/v0.8自动验证结果.md` 和 `validation/v0.8自动验证结果.json`。

## 三、规模验证

夹具规模：

- 20 个 Goals；
- 50 个 TaskGroups；
- 500 个 Assignments；
- 30 个 CalendarConstraints；
- 10 个本地 PlanVersions。

当前容器测量：

| 指标 | 结果 |
|---|---:|
| 轻量活动状态连续克隆 50 次 | 约 50.93 ms |
| 轻量活动状态序列化 100 次 | 约 55.73 ms |
| 云端可移植状态大小 | 189,451 bytes |
| 错误内嵌 10 个重型版本后的大小 | 4,523,120 bytes |
| 重型/轻量体积比 | 23.87 倍 |

验证结论：

- 规定规模夹具生成正确；
- 云端状态不包含 `beforeState/afterState` 重型版本内容；
- 版本快照没有递归嵌套版本；
- 序列化可完成；
- 本地重型版本与普通自动保存分离具有明确体积收益。

测量仅用于当前运行环境的相对验证，不应作为所有手机的绝对性能承诺。

## 四、关键回归保护

### 执行状态

- 已完成任务不被自动移动；
- 正在计时任务不被自动移动；
- 部分完成保留实际用时，排期使用剩余工作量；
- 重新打开已完成任务保留实际用时；
- 过去冻结，今天按实际和推定用时半冻结。

### 用户意图

- 手动移动写入 `intentStrength = manual`；
- 锁定任务为永久硬约束；
- 手动安排在最小扰动方案中受到高权重保护；
- 必须移动手动任务的方案会明确标识；
- 用户优先级不会被系统静默修改。

### 约束

- 日容量、任务组每日上限、活动类型上限、90 分钟长任务、高强度任务和日期保护共用同一评估核心；
- 来源日与目标日均进行校验；
- 无合法日期时任务保持未安排，不强塞；
- 一次性例外不改变永久默认值；
- 时长证据只能提出上限调整建议。

### 数据

- v0.7 迁移不触发重排；
- 旧目标日期只用于迁移；
- 任务组数量从实际任务记录归一；
- 重大操作创建本地版本；
- 云端不携带重型历史；
- 游客和账号命名空间隔离。

## 五、需求中明确不实现的内容

以下不是缺陷，而是冻结需求明确延期/排除：

- AI 自然语言功能；
- 独立 Milestone；
- 独立 Execution Drift；
- 独立 AI 时长预测服务；
- 学习质量、正确率和掌握度评价；
- 通用依赖图和自动任务拆分；
- 正式 Cancelled 状态；
- 云端完整计划版本历史。

代码只预留 `PlanIntentParser.parseUserIntent` 接口，未来 AI 也只能产生变化事件草稿，不能直接应用计划。

## 六、当前环境未完成的验证

### 1. 真实生产构建

当前容器的内部 npm 镜像缺少 `@supabase/supabase-js`，切换公共 npm 源也无法联网完成请求。因此没有声称 `npm run build` 已通过。

已完成替代验证：

- 系统 TypeScript 5.8.3；
- 项目严格 `tsconfig.check.json`；
- 全量源码类型检查通过；
- Web Worker 文件也包含在类型检查范围中。

在可访问正常 npm 源的环境中仍需执行：

```bash
npm ci
npm run validate:v08
npm run build
```

### 2. iPhone PWA 真机

代码已覆盖 standalone、安全区、100dvh、七列月历、全屏复杂模态、底部抽屉、无横向溢出和长文本换行。仍需在至少一台带 Home Indicator 的 iPhone 上检查：

- 添加到主屏幕后启动；
- 月/周切换；
- 长按移动与页面滚动不冲突；
- 日期详情和方案全屏；
- 键盘弹出后的数字输入与底部按钮；
- 横竖屏和字体放大。

### 3. Supabase 真实项目

需要真实 URL、匿名密钥、用户账号和网络环境验证：

- 注册/登录；
- 首次账号起点选择；
- 游客导入和拒绝导入；
- 多设备当前计划同步；
- 串行合并写入；
- 旧请求不能覆盖新状态；
- 本地版本不上传。

## 七、发布前最终验收清单

1. 在正常 npm 环境执行 `npm ci && npm run validate:v08 && npm run build`。
2. 使用 v0.7 实际导出数据做一次迁移并人工抽查。
3. 在 iPhone 主屏幕 PWA 完成移动端场景。
4. 使用测试 Supabase 项目完成账号/游客/多设备同步。
5. 在浏览器 Performance 中以 500 项真实界面数据检查编辑、滚动和方案计算。
6. 确认方案计算取消、失败和重试都不改变当前计划。
7. 确认恢复计划版本前后实际用时不减少。

## 八、最终判定

- 需求实现：**完成**。
- 自动代码验证：**通过**。
- 规模与存储结构验证：**通过**。
- 真实依赖生产构建：**待外部环境验证**。
- iPhone PWA 真机：**待外部环境验证**。
- Supabase 端到端：**待外部环境验证**。

交付物适合作为 v0.8.0 候选发布源代码；在完成上述三类外部验收后，可标记为正式生产发布。
