# Study Planner v0.8.13 全局手机弹窗底部结构修复报告

## 问题

手机端多个弹窗把操作按钮放在滚动正文内部，再通过 `position: sticky` 模拟底部栏，导致：

- 按钮悬在正文上方，正文会从按钮后面滚过；
- 按钮与底部安全区之间出现不自然空白；
- 不同弹窗分别维护粘性规则，层级和按钮排列不一致；
- 两按钮和三按钮弹窗在手机上经常纵向占据过多空间。

## 修复

### 统一弹窗结构

所有弹窗统一为：

```text
固定标题栏
独立滚动正文
真实底部操作栏
```

`Modal` 会自动识别其内容末尾的 `.modal-actions`，把该操作区从正文中提取到同级 `modal-footer`。已有显式 footer 的方案预览继续使用原 footer，不重复提取。

### 统一移动端按钮排列

- 两个按钮：同一行，取消按钮较窄，主操作占据剩余空间；
- 三个按钮：两个次级操作在第一行，主操作单独占满第二行；
- 一个按钮：占满整行；
- footer 负责底部安全区，正文不再额外为按钮预留大块空白。

### 统一旧样式兼容

在真实 footer 中统一取消旧的：

- `position: sticky`；
- 负边距；
- 半透明背景；
- 正文内安全区补偿；
- 页面级重复底部栏样式。

## 覆盖范围

自动覆盖所有以 `.modal-actions` 结尾的弹窗，包括：

- 计划调整；
- 添加单项任务；
- 创建或编辑任务组；
- 创建或编辑目标；
- 日期约束；
- 更换任务组；
- 完成/部分完成；
- 批量顺延；
- 批量移动；
- 导入与恢复；
- 计划版本恢复；
- 计时结束相关弹窗。

## 验证

### 真实 Chromium 390×844

双按钮弹窗：

- 弹窗：`0 → 844`；
- 正文底部：`776`；
- footer 顶部：`776`；
- footer 底部：`844`；
- 正文/footer 重叠：`0px`；
- footer/视口底部空隙：`0px`。

三按钮弹窗：

- 正文底部与 footer 顶部完全相接；
- footer 底部与视口底部完全相接；
- 重叠：`0px`。

桌面 1200×900：

- 正文与 footer 重叠：`0px`；
- 操作区保持右对齐、紧凑布局。

### 回归

- TypeScript 严格检查：通过；
- 综合自动验证：76 / 76；
- 核心场景：44 / 44；
- UI 审计：28 / 28；
- 用户效率验证：22 / 22；
- 移动端复盘专项：通过；
- 移动端方案预览专项：通过；
- 全局移动弹窗专项：10 / 10；
- 500 项规模测试：通过。

## 数据兼容

本轮不修改 AppState、schemaVersion、调度算法、迁移逻辑或用户数据，只修改弹窗承载结构、样式和验证脚本。
