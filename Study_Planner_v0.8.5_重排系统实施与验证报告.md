# Study Planner v0.8.5 重排系统实施与验证报告

## 一、最终实施路线

本次没有回到 v0.7.0 重复实现 v0.8 功能，而是：

- 以 v0.8.4 为代码基础；
- 以 v0.7.0 为稳定行为基线；
- 保留 Goal、TaskGroup、Assignment、CalendarConstraint、PlanVersion、迁移、同步和底层约束核心；
- 重构事件进入调度器之前的“计划调整协调层”；
- 重构计划调整预览的信息层级与手机布局。

## 二、核心产品原则落实

1. 用户已经明确作出的决定，只做完整校验，不重新替用户决定。
2. 系统准备执行的智能方案或改动，必须先预览再应用。
3. 系统提供推荐方案和实质不同的备选，最终由用户决定。
4. 默认界面简洁；数量可以展开到具体项目，项目可以继续展开到原因和计算依据。
5. 移动端不通过删除信息换取简洁，而是使用折叠、单列、全屏和自然换行。
6. 同一含义在目标、任务、日历、复盘和时长建议中使用一致的视觉和交互。

## 三、计划调整信息架构

```text
业务模块发生变化
→ 计划调整协调器识别场景和用户意图完整度
→ 精确校验 / 推荐预览 / 可选优化 / 探索式优化
→ 共用约束与目标计算核心
→ 推荐方案 + 实质不同的备选
→ 第一层摘要
→ 点击计数展开具体问题、任务、日期、目标
→ 点击项目查看原因、约束和其他日期解释
→ 用户确认
→ 原子应用并创建可恢复版本
```

## 四、事件决策矩阵

| 事件 | 处理模式 | 默认结果 |
|---|---|---|
| 复盘逐项顺延 | 精确校验 | 合法项固定，只处理冲突项 |
| 批量移动 | 精确校验 | 按用户指定日期预览，不重算全部任务 |
| 时长更新且日期仍合法 | 精确校验 | 更新预计，日期保持不变 |
| 新单项任务/任务组 | 推荐预览 | 零移动插入优先，必要时最低扰动 |
| 补充缺失子任务 | 推荐预览 | 只创建并安排新增项 |
| 目标提前或条件收紧 | 推荐预览 | 只调整满足新条件所需任务 |
| 旅行或容量下降 | 推荐预览 | 搬出不可用范围并平衡前后 |
| 目标放宽或删除 | 可选优化 | 默认保持现有排期 |
| 容量增加或解除限制 | 可选优化 | 默认保持现有排期 |
| 计划太累 | 探索式优化 | 先选择具体减负结果 |
| 主动重新组织未来 | 探索式优化 | 明确取舍后展示推荐和备选 |

## 五、最初反馈问题的覆盖结果

### 1. 手机端大块空白

- 手机复杂弹窗使用 `100dvh` 弹性全屏结构；
- 标题区、正文滚动区和底部操作区分离；
- 安全区只计算一次；
- 修复内容区高度和双重安全区导致的空白；
- 月历取消移动端强制宽度，完整保留七列。

### 2. 复盘 9 项选择后再次重排

- 复盘已选日期进入精确预览；
- 合法选择写入固定集合，不再作为调度候选；
- 只从精确校验问题中提取冲突项；
- 删除复盘中的重复“比较完整调整方案”入口；
- 运行级测试验证合法日期在冲突修复中保持不变。

### 3. 重排中心信息混乱

- 第一屏只显示变化、可行性、推荐、影响数量和主要风险；
- 推荐方案默认展开；
- 其他方案由用户主动比较；
- 问题、任务、日期、目标、例外和计算依据分别折叠；
- 不再要求所有事件先选择四种抽象算法偏好。

### 4. 四个选项选中态过浅

选中状态同时使用：

- 明显边框；
- 背景变化；
- 阴影；
- “已选择”文本标记。

桌面和手机使用同一语义。

### 5. 日期负载变化缺少红绿提示

- 负载增加：红色向上；
- 负载减少：绿色向下；
- 无变化：中性；
- 容量增加与减少单独显示；
- 日期卡同时显示变更前后分钟、任务数和容量。

## 六、主要实现文件

- `src/lib/adjustment.ts`
- `src/lib/planner.ts`
- `src/App.tsx`
- `src/components/ProposalDialog.tsx`
- `src/components/AdjustmentIntentDialog.tsx`
- `src/components/ReviewDialog.tsx`
- `src/styles.css`
- `src/types.ts`
- `scripts/scenario-v085.mjs`
- `scripts/runtime-adjustment-v085.mjs`
- `scripts/validate-v08.mjs`

## 七、自动验证结果

- TypeScript 严格检查：通过。
- 综合要求验证：62 / 62。
- 核心场景验证：27 / 27。
- 复盘精确校验运行测试：通过。
- 500 项任务规模测试：通过。

规模测试包含 20 个目标、50 个任务组、500 项任务、30 个日期约束和 10 个本地计划版本。

## 八、生产构建限制

当前执行环境没有项目 `node_modules`，且不能完整安装 React、Vite、date-fns、Supabase 等外部依赖，因此 `npm run build` 因模块不可解析失败。该失败不是被隐藏的源码验证结果：独立严格 TypeScript 检查、运行级调度测试、场景测试和规模测试均已通过。

部署环境仍需执行：

```bash
npm install
npm run typecheck
npm run validate:v08
npm run build
```

## 九、完成性评估

### 已完成

- 场景协调层；
- 精确预览；
- 推荐与备选；
- 渐进式信息层级；
- 复盘重复重排修复；
- 目标收紧/放宽分流；
- 任务插入和补子任务分流；
- 日期容量收紧/放宽分流；
- 时长修正分流；
- 手机全屏布局、七列月历、选中态、红绿负载；
- 自动测试与文档追踪；
- 完整源码包与覆盖补丁包。

### 仍需真实环境验收

- 安装真实 npm 依赖后的 Vite 生产构建；
- iPhone Safari/PWA 真机视觉和触摸回归；
- Supabase 实际账号、跨设备同步和冲突场景；
- 真实用户数据上的主观易用性观察。

这些项目需要真实浏览器、手机、网络和后端环境，当前容器无法伪造其通过状态。
