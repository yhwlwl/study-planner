# Study Planner v0.8.9 用户操作优先实施与验证报告

## 需求

用户删除一个任务时，系统不得因为计划原本存在硬冲突而自动把操作扩大为移动 17 项任务。第一方案必须只完成删除。

## 实现

- 新增最小作用域事件元数据。
- 单任务和任务组删除改为准备态预览。
- 删除直接影响任务组数量、目标引用和隐藏独立组清理。
- 第一方案固定为精确预览，不启动 Worker 或调度搜索。
- 既有硬问题通过结构化约束身份进行增量比较。
- 方案页面显示原有问题、已解决、已缓解以及新增/恶化数量。
- 用户主动获取更多方案后才扩大为优化模式。

## 运行验证

构造一个原本已有 3 个硬问题的计划，然后删除一个无关任务：

- 删除预览可执行；
- 其他任务移动：0；
- 识别原有硬问题：3；
- 本次新增或恶化：0；
- 删除任务出现在结构变化明细中。

## 自动验证

- TypeScript 严格检查：通过；
- 综合验证：75/75；
- 场景验证：40/40；
- UI 审计：17/17；
- 500 任务规模测试：通过；
- 生产构建：当前容器缺少真实 npm 依赖，未标记为通过。
