# Study Planner v0.8.9 覆盖补丁说明

## 基线

本补丁用于覆盖完整 v0.8.8 源码。

## 覆盖内容

- 用户操作优先与三层作用域；
- 单任务、任务组删除预览；
- 既有问题与新增/恶化问题分账；
- 作用范围桌面/手机 UI；
- v0.8.9 自动验证脚本和文档。

## 使用

将补丁内文件按原目录覆盖到 v0.8.8，然后执行：

```bash
npm install
npm run typecheck
npm run build
```
