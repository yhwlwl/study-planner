# Study Planner v0.8.7 对话需求变更总表

**冻结日期：** 2026-07-31  
**实现基线：** v0.8.6  
**稳定行为参考：** v0.7.0  
**本轮目标：** 修正复盘完成后的重复决策、已完成历史被误判为冲突、冲突选项层级混乱等问题，并继续落实“外表简洁、内在丰富、系统改动先预览、用户最终决定”。

## 1. 不变的最高原则

1. 算法负责计算，系统负责解释，用户负责决定。
2. 任何系统准备执行的任务移动、规则例外、目标修改、容量修改或结构变化，都必须先预览。
3. 系统应提供多个有实质差异的方案，但不把所有内容一次堆在首屏；默认展示推荐结论，用户按需展开明细或获取更多方案。
4. 所有汇总数量都可以展开到具体项目；具体项目还可以继续展开到原因、涉及任务、计算依据和未采用其他日期的原因。
5. 手机端不能通过删除信息来解决拥挤；应使用单列重排、折叠明细、全屏复杂预览、安全区和自然换行。
6. 用户已经明确作出的决定只能校验，不能被系统重新清空并替用户决定。
7. 已完成和已发生的执行记录是历史事实，不能被重排、撤销或作为待处理冲突阻塞当前操作。

## 2. 计划调整中心的职责

计划调整中心是统一的影响分析、方案比较和执行预览层，不是所有业务操作都必须先经过的提问中心。

业务模块先表达变化：

- Goal：日期收紧、放宽、完成条件变化、阶段检查；
- TaskGroup/Assignment：新增任务、新增任务组、补子任务、修改规则；
- CalendarConstraint：旅行、不可用、容量下降或增加；
- Review：未完成任务处理、真实执行差异、预计时长建议；
- Manual Move：用户指定日期或批量移动；
- Plan Preference：计划太累、希望减少连续高负载、希望重新组织未来。

协调器再根据意图完整度分流：

| 模式 | 使用场景 | 默认行为 |
|---|---|---|
| 仅提交 | 不影响排期的普通编辑 | 直接保存 |
| 校验后提交 | 用户已明确日期或结果 | 快速验算，通过后直接提交 |
| 推荐预览 | 用户给出目标或新约束，日期交给系统 | 展示推荐方案，允许比较备选 |
| 可选优化 | 当前排期仍合法，优化非必需 | 默认保持现状，优化由用户主动选择 |
| 探索式优化 | 用户只表达“太累”等感受 | 先询问期望结果，再生成方案 |

## 3. 复盘完成后的三条路径

用户在复盘中已经逐项选择未完成任务的处理日期，这些选择本身就是一套完整方案。复盘完成页必须提供三条明确路径：

### 3.1 完成复盘，并按当前方案顺延 X 项

- 展示将移动的任务数量、涉及日期、是否移动其他任务、目标是否仍可完成；
- 只执行快速完整校验，不重新搜索日期；
- 全部合法时直接原子提交；
- 部分冲突时固定合法项，只让用户处理冲突项；
- 不再自动进入完整计划调整中心。

### 3.2 获取更多方案

- 只有用户主动点击后才运行完整调度；
- 复盘中已选方案必须作为方案 A 保留；
- 系统可生成移动更少、负载更均衡、目标更早或休息更多的备选；
- 所有备选仍需完整预览后才能应用。

### 3.3 仅保存复盘，暂不顺延

- 保存完成状态、真实用时、复盘记录和时长建议；
- 不执行当前顺延草稿；
- 页面明确说明未完成任务尚未顺延，避免造成已安排的错觉。

## 4. 已完成历史与当前冲突的边界

### 4.1 已完成历史不再是待处理冲突

以下情况即使超过原规则，也不显示为需要用户解决的硬冲突：

- 已经完成 9 小时，超过当天容量；
- 已经完成 5 套数学卷子，超过每天 1 套的上限；
- 已完成任务数量超过高强度、长任务或同类任务上限；
- 历史计划本来已经超载，而当前方案没有新增或恶化该超载。

原因：这些都是不可修改的历史事实，系统无法换日，也不应阻止保存复盘或应用其他调整。

### 4.2 历史事实仍参与后续校验

已完成历史不能被忽略。它仍然用于：

- 当天真实负载；
- 同类任务实际完成数量；
- 长任务和高强度任务计数；
- 判断还能否向当天新增或保留未完成任务。

例如当天已经完成 9 小时，系统不显示“9 小时历史超载需要处理”；但如果当前方案还准备把一个未完成任务留在当天，则只针对这个仍可调整任务提示：

- 换到其他日期；
- 本次接受继续安排；
- 暂不安排；
- 修改当天容量。

### 4.3 只检测新增或恶化的冲突

比较方案前后时按结构化约束事实比较，而不是比较提示文字。若原计划为 609 分钟、当前方案减少为 594 分钟，即使仍高于容量，也不能因为文字中的数字变化而误判为“新冲突”。

新冲突成立的条件包括：

- 原来合法，方案后违法；
- 方案使超出量增大；
- 方案把新的未完成任务放入已有非法日期；
- 方案新增了需要用户授权的规则例外。

## 5. 冲突分类与逐项决策

冲突不能只用一个 `infeasible` 状态表示，应区分：

| 类别 | 例子 | 处理原则 |
|---|---|---|
| 绝对阻断 | 移动已完成任务、过去日期、正在计时任务 | 不能豁免，必须保持或先结束相关状态 |
| 用户保护 | 锁定任务、手动安排、保护日期 | 明确解锁、允许移动或一次性使用 |
| 可一次性放宽 | 容量、每日同类上限、长任务、高强度上限 | 可逐项接受一次性例外 |
| 目标/结构问题 | 最晚日期不可满足、完成条件无合法容量 | 修改目标、条件、容量或保留未安排 |
| 普通提醒 | 期望日期风险、负载偏高、连续高负载 | 不阻断，可继续应用 |

### 5.1 选项按用途分层

不同冲突允许的操作数量可以不同，但必须放入固定层级，不能把不同性质的动作混为平级方案。

**处理当前任务：**

- 接受本次例外；
- 让系统仅为涉及任务换日；
- 暂不安排这些任务；
- 保持指定日期或原日期。

**修改产生冲突的条件：**

- 修改可用时间；
- 修改目标日期或完成条件；
- 修改任务组规则。

**不继续这部分调整：**

- 恢复这些任务原来的日期；
- 放弃本次对这些任务的调整；
- 取消整个变化。

### 5.2 部分接受后必须重新计算

用户可以对多个冲突分别决定。例如接受当天增加 30 分钟，但拒绝化学预习每日上限例外。系统必须：

1. 保存已接受的精确授权；
2. 保持用户明确拒绝的规则；
3. 只释放需要重新寻找位置的任务；
4. 从当前选中候选继续计算；
5. 展示新的完整预览；
6. 若产生新冲突，继续让用户处理；
7. 最终无未决问题后才能应用。

### 5.3 一次性例外采用最小授权

例外必须精确到：

- 本次事件；
- 日期；
- 原始规则；
- 涉及任务；
- 原限制和本次临时上限。

它不能变成整日无限制权限，也不能修改永久设置。

## 6. 多方案与简洁界面的平衡

第一屏只回答：

1. 发生了什么；
2. 当前计划是否可执行；
3. 推荐怎样解决；
4. 影响多少任务和日期；
5. 最大代价或风险是什么。

默认展示一个推荐方案。用户可点击：

- 查看 N 个问题；
- 查看 X 项任务变化；
- 查看 Y 个日期负载变化；
- 比较其他方案；
- 生成更多有实质差异的方案。

每个数字都可展开，信息不删除，只按摘要、列表、单项详情、计算依据逐层呈现。

## 7. UI 与手机端要求

1. Today 头部桌面双列、手机单列，真实内容决定高度，不使用会在纵向布局中变成高度的 `flex-basis`。
2. 复杂计划调整在手机端使用 `100dvh` 全屏弹性布局，标题和底部操作固定，正文独立滚动。
3. 所有卡片与 Grid 子项设置正确的 `min-width: 0`，任务名、日期和原因自然换行。
4. 选中态同时使用明显边框、背景、阴影和“已选择”标记。
5. 日期负载增加使用红色向上提示，减少使用绿色向下提示；无变化中性；超载恢复正常应强化绿色。
6. 主按钮不能因存在冲突而无解释地全部失效：应显示“处理 N 个问题”“按选择重新计算”“查看不能执行的原因”等下一步。
7. 简单决定一步完成；普通影响一次展开；完整依据再深入一层，避免为了渐进展示而制造无意义点击。

## 8. 典型场景对应规则

- 目标 8 月 8 日提前到 8 月 6 日：直接分析可行性，推荐最小必要调整；不可行时展示容量缺口和可选结构变化。
- 化学预习放宽到 8 月 20 日：默认保存目标并保持当前排期，减负是可选优化。
- 任务组缺少子任务：只创建缺少项，先零移动插入，再低扰动调整。
- 计划太累：先询问希望降低每日量、增加轻量日、减少连续高负载或分散长任务，再生成方案。
- 8 月 10–15 日出游：创建一个范围约束，列出受影响任务，平衡旅行前后，不能全部堆到 8 月 16 日。
- 新增任务组：生成草稿并预览推荐插入；允许创建为未安排。
- 老师 8 月 15 日检查：使用 Goal 的数量/百分比条件，只提前满足检查所需任务。
- 时长自动修正：日期仍合法时只更新预计；新增冲突时只做最小修复。
- 复盘顺延：用户当前方案可直接提交；更多方案由用户主动请求。

## 9. 验收重点

- 已完成 9 小时和 5 套卷子不会出现为待处理冲突；
- 当前方案向该日新增未完成任务时仍会被正确校验；
- 原计划已有超载但方案减少负载时不会误判为新冲突；
- 复盘完成页存在三条明确路径；
- 直接应用当前复盘方案不运行完整多方案调度；
- 获取更多方案时保留当前方案作为比较基线；
- 合法复盘任务固定，只处理冲突任务；
- 冲突选项按用途分组，文案能说明后果；
- 部分接受/拒绝后重新计算并再次预览；
- 所有系统改动在应用前可见；
- 手机端无大块空白、横向溢出或不可辨认的选中态。
