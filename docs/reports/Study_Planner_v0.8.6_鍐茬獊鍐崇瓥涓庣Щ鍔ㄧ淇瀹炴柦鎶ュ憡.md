# Study Planner v0.8.6 冲突决策与移动端修复实施报告

## 实施结论

本轮没有把“硬冲突”简单改成可勾选后强行应用，而是建立：冲突分类、逐项决定、最小范围例外、带条件重新计算、二次完整预览和最终应用的闭环。

## 手机首页空白

根因是桌面端 `flex: 1 1 350px` 在手机端 `flex-direction: column` 后转化为约 350px 高度。现改为桌面双列、手机单列 Grid，内容高度由真实内容决定。

## 冲突分类

- 绝对阻断：已完成历史、过去日期、正在计时；不能豁免。
- 用户保护：任务锁定、日期保护；需要保留、解锁或本次明确授权。
- 可一次性放宽：容量、任务组/活动每日上限、长任务和高强度上限。
- 目标或结构冲突：最晚日期、无合法位置；需要换日、留空、修改目标或容量。
- 提醒：不阻断，但完整展示影响。

## 部分接受与重新计算

用户可对每项冲突分别选择。接受的例外记录日期、原始规则、临时上限和涉及任务；拒绝的例外只释放其涉及任务。系统从用户当前选中的候选继续，保留其余合法移动与微调，随后重新计算并再次展示任务、日期、负载、目标和例外预览。

## UI

默认只显示冲突数量和分类；点击后显示具体问题；继续展开可查看涉及任务、当前值、允许值、后果与解决方式。主按钮根据状态显示“处理 N 个问题”“按这些选择重新计算”或“应用预览中的改动”。

## 验证

- 综合验证：68 / 68
- 核心场景：33 / 33
- 逐项冲突运行测试：通过
- 500 项任务规模测试：通过
- TypeScript 严格检查：通过
- 生产构建：因当前 npm 镜像缺少 Supabase 包而未完成，未冒充通过
