# Study Planner v0.8.10 覆盖补丁说明

## 基线

此补丁用于覆盖完整 v0.8.9 源码，不适用于只含静态构建产物的目录。

## 覆盖方法

1. 备份现有源码和用户 JSON；
2. 将补丁包内文件按原目录覆盖到 v0.8.9；
3. 执行 `npm install`；
4. 执行 `npm run typecheck`、`npm run test:efficiency` 和 `npm run build`；
5. 使用同一生产域名部署，以继续读取同源 IndexedDB 数据。

## 主要内容

- 计时完成防丢失；
- 统计残差只计一次；
- Today 口径统一；
- 具体任务收件箱；
- 复盘暂不顺延任务可找回；
- 删除、顺延、导入和恢复流程去重；
- 批量移动日期选择器；
- 高级设置渐进折叠；
- 即时撤销和草稿保护；
- v0.8.10 验证脚本与文档。

## 数据

本轮不提升 schema，v0.7 与 v0.8.9 数据继续由 schema 9 的既有迁移和规范化代码处理。
