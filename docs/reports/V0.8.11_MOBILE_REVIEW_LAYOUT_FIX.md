# v0.8.11 Mobile Review Layout Fix

The final mobile cascade now explicitly overrides the later desktop review-decision grid. At widths up to 760px, `.review-task-decision` becomes a vertical flex container; its task content, progress, and carry-date selector each receive the full available width. A rendered 390px viewport fixture is included under `validation/`.
