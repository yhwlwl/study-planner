# Study Planner v0.8.7 覆盖补丁说明

本补丁以完整 v0.8.6 源码为基线，不适用于更早版本直接覆盖。

## 主要变化

- 复盘完成页改为“按当前方案顺延 / 获取更多方案 / 仅保存复盘”三条路径。
- 当前复盘方案合法时直接提交，不再运行完整多方案调度。
- 已完成历史超容量、超次数不再生成待处理硬冲突。
- 历史负载仍用于校验新的未完成任务。
- 新旧冲突按结构化事实比较，避免非恶化旧超载被误判。
- 冲突操作按处理任务、修改条件和撤销调整分层。
- 新增 v0.8.7 场景与运行级验证。

## 覆盖方式

1. 备份当前项目和数据。
2. 将补丁 ZIP 解压到 v0.8.6 项目根目录并允许覆盖。
3. 在正常 npm 网络环境执行：

```bash
npm install
npm run typecheck
npm run validate:v08
npm run build
```
