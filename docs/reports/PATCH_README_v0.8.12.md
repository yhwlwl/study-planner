# Study Planner v0.8.12 覆盖补丁说明

适用基线：v0.8.11。

本补丁只修改计划调整预览布局与通用 Modal 的可选 footer 支持，不改变数据库结构、迁移版本或调度算法。

主要变化：

- 单一局部操作直接展示本次结果；
- 顶部信息去重；
- 底部按钮不再遮挡正文；
- 主按钮使用具体操作名称；
- 手机全屏最终固定为 100dvh；
- 新增移动端方案预览验证。
