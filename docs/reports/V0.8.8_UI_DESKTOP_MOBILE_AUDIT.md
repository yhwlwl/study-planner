# Study Planner v0.8.8 桌面与手机界面审计

## 1. 审计原则

- 桌面端强调信息分区、横向利用和可扫描性，不把手机单列直接放大。
- 手机端强调单列顺序、触控面积、安全区和完整换行，不把桌面多列强行压缩。
- 默认展示摘要，具体任务、日期、目标和计算依据按需展开。
- 视觉简洁不能删除解释和用户决策能力。

## 2. 用户截图中确认的问题

### 复盘页

- 顶部完成率英雄区过大，装饰占据过多高度。
- 六张指标卡同排过窄，长时间数字被迫换行。
- 指标卡、任务处理区和最终决策区之间层级不够清晰。
- 桌面空间虽宽，但信息仍像放大的手机页面。

### 计划调整入口

- 三个变化原因卡和“今天还能接收多少任务”缺少主次关系。
- 横向分段控件只占页面一部分，右侧留下大面积空白。
- 保护规则和主要输入混在同一纵向流中，扫描成本较高。
- 底部操作区与正文之间缺少稳定的桌面边界。

## 3. v0.8.8 结构调整

### 复盘

- 独立 `review-modal`，桌面最大宽度 1160px。
- 桌面指标改为三列两行，手机保持两列。
- 缩小纯装饰面积，保留完成率、计划/实际时间和关键差异。
- 任务处理、已完成任务、时长建议、图表和最终决定保持明确顺序。
- 最终三条路径继续保留：按当前方案完成、获取更多方案、仅保存复盘。

### 计划调整入口

- 独立 `adjustment-modal`，桌面最大宽度 1060px。
- 桌面使用“主内容 + 侧栏”：左侧选择变化原因和结果偏好；右侧设置今天容量和查看保护规则。
- 手机自动回落为单列，不保留桌面侧栏。
- “今天还能接收多少任务”改为 2×2 卡片，消除横向空白。
- 顶部明确显示“改动先预览、方案可比较、决定权归用户”。

### 方案预览

- 独立 `proposal-modal`，桌面最大宽度 1120px。
- 方案选择使用自适应卡片；摘要桌面四列、手机两列。
- 任务、日期、目标、例外和计算依据继续渐进展开。
- 冲突决策和底部操作保持可见且不遮挡正文。

## 4. 其他页面审计

| 页面/模块 | 桌面 | 手机 |
|---|---|---|
| Today | 日期摘要与操作横向分区 | 内容高度自适应，操作按钮网格化 |
| 月历 | 七列完整展示，保持信息密度 | 七列压缩到视口，不强制横向滚动 |
| 全部任务 | 工具栏和任务组卡片保持横向扫描 | 工具栏换行，任务动作折叠 |
| Goals | 两列目标卡片 | 单列目标卡片 |
| Statistics | 多列 KPI 与双栏图表 | KPI 两列/单列，图表纵向排列 |
| Settings | 左说明、右表单双栏 | 单列表单 |
| 日期约束 | 内容与操作双区 | 单列并允许按钮换行 |
| 历史差异 | 日期卡与前后差异并列 | 前后内容上下堆叠 |
| 创建/编辑弹窗 | 双列表单 | 单列表单和底部安全区 |

## 5. 验证

- UI 静态审计：15 / 15。
- 复杂弹窗桌面独立滚动：通过。
- 手机 `100dvh`、安全区和单列回落：通过。
- 全量业务场景：36 / 36。
- 综合验证：72 / 72。
