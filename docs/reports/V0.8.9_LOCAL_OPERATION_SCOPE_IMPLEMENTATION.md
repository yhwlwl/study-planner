# v0.8.9 用户操作优先与最小作用域实现

## 1. 原则

明确局部操作采用“用户操作优先，系统优化后置”：

1. 第一方案只执行用户明确要求；
2. 不移动其他任务；
3. 计划原有问题只提示，不阻止本次操作；
4. 只有本次新增或恶化问题进入冲突处理；
5. 用户主动获取更多方案后，才扩大作用范围。

## 2. 作用域

- `requested-change-only`：只执行用户操作；
- `directly-affected-items`：只处理直接受影响任务；
- `broader-future-plan`：用户主动请求未来优化。

## 3. 事件

新增 `assignment-deletion`，并为以下事件写入 `explicitLocalOperation`：

- 单任务删除；
- 任务组删除；
- 目标删除；
- 任务换组；
- 任务组编辑。

## 4. 增量冲突比较

`ProposalIssueDelta` 记录：

- 操作前既有硬问题；
- 操作后仍存在的既有问题；
- 本次解决的问题；
- 本次缓解的问题；
- 本次新增或恶化的问题。

只有最后一类属于本次必须处理的冲突。

## 5. 删除流程

```text
点击删除
→ 生成只删除目标对象的准备态
→ 比较操作前后约束事实
→ 展示结构、目标和统计影响
→ 其他任务移动为 0
→ 用户确认应用
```

用户点击“生成更多方案”后，事件才临时提升为 `optimize`，以生成更广泛的可选优化。
