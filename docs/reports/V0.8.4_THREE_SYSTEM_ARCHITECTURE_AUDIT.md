# Study Planner v0.8.4：计划调整、复盘、目标三大系统架构复核

## 结论

本轮不是只确认“页面和类型是否存在”，而是沿着真实操作链检查：用户变化是否形成事件、算法是否共用同一约束核心、正式计划是否只在确认后改变、历史是否能恢复，以及目标与复盘是否真正参与调度。

v0.8.3 的总体方向已经正确，但仍有三类不完整闭环：

1. 方案中的单项改期只能查看，不能在同一预览内保留原日、自定义日期或锁定并重新验算；
2. 复盘主要按计划日期取任务，跨日实际执行可能遗漏，过去复盘还可能被后续重开或改期重写；
3. 目标缺少可靠的实际达成日期与按期结果，空目标可能自动完成，进行中目标还能通过归档绕过约束。

这些问题已在 v0.8.4 修正。当前三大系统符合此前确认的目标架构。

---

# 一、计划调整系统

## 1. 最终架构

```text
变化事件（发生了什么）
→ 内部动作（Insert / Repair / Optimize / Rebuild）
→ 用户偏好（尽量少改 / 均衡执行 / 目标优先 / 更多休息）
→ 共用约束与目标评估核心
→ 多个可解释方案
→ 方案内逐项微调并重新验算
→ 用户应用或保持不变
→ 重大变化形成计划版本
```

这不是“局部修复”和“全面重排”两套系统。旧 `repair/full` 只保留为底层搜索范围兼容适配器，不再作为用户首先选择的产品概念。

## 2. 变化事件覆盖

已覆盖：

- 新单项任务；
- 新任务组；
- 任务组补充或减少子任务；
- 目标提前、放宽、条件或优先级变化；
- 旅行、休息、降低容量和增加空余时间；
- 今日未完成和执行差异；
- “当前计划太累”；
- 预计时长或每日规则变化；
- 用户主动重新组织未来计划。

## 3. 约束体系

永久保护：

- 过去日期；
- 已完成任务；
- 正在计时任务；
- 锁定任务；
- 日期保护；
- 不删除任务；
- 目标最晚条件。

需要明确一次性确认才能放宽：

- 每日容量；
- 同类任务每日上限；
- 长任务上限；
- 高强度上限；
- 受保护缓冲日。

软约束进入评分：

- 期望完成日期；
- 手动安排保护；
- 每日均衡；
- 科目和强度搭配；
- 休息节奏；
- 移动数量和距离。

## 4. v0.8.4 补齐

- 每个移动任务现在可在方案内选择“保留原日期”、自定义目标日期和“锁定这个结果”。
- 每次逐项修改都会重新计算容量、每日上限、目标风险、日期保护、任务移动、日期负载和稳定性，不能绕过调度器。
- 稳定性评分现在真实包含移动数量、移动距离、手动任务、影响日期、负载变化、目标风险和保护日期，而不是只有文案声称。
- “检测到的问题”与“仍未解决的问题”语义分离，不再把调整前问题误称为调整后仍待处理。
- 方案明确保留一次性例外；逐项微调会继续尊重用户已经确认的候选例外和历史既有占用。

## 5. 场景结果

- 8 月 8 日目标提前至 8 月 6 日：从当前可调整未来开始寻找位置，而不是把 8 月 6 日错误当作搜索起点。
- 化学目标放宽到 8 月 20 日：可保持早完成、减轻每日负载或释放容量，不自动推迟。
- 8 月 10–15 日旅行：可把任务安排到旅行前或旅行后，不集中倾倒到 8 月 16 日。
- 计划太累：成为正式 `load-preference-change` 事件，内部使用 Optimize + Rest。
- 增加可用时间：提供保持现状或利用空余减负，不自动把未来任务提前。

---

# 二、复盘系统

## 1. 最终架构

```text
结束今天并复盘
→ 当日计划 + 当日真实执行快照
→ 摘要数据
→ 未完成任务处理
→ 自适应时长建议
→ 按需展开图表
→ 保持、顺延预览或时长调整预览
→ 保存复盘记录
```

复盘不是新的重排引擎。它产生“执行差异”或“预计时长变化”事件，再调用统一方案引擎。

## 2. 显示结构

默认只显示：

- 完成数与完成率；
- 计划时间；
- 实际时间；
- 推断负载；
- 预计差异；
- 未完成任务数量；
- 时长建议数量。

所有摘要数字均可展开。统计图表保持次级，不做装饰性图标系统。

## 3. 未完成任务闭环

- 保留原“结束今天”的顺延与保留逾期能力；
- 锁定或正在计时任务不能移动；
- 简单顺延先进入草稿；
- 组合后的容量、每日上限、目标日期和日期保护在应用前重新检查；
- 复杂情况可进入完整计划调整方案。

## 4. 自适应时长

- 使用最近 10 个有效样本；
- 样本不足时不主动建议；
- 使用稳健异常值处理；
- 历史只提出新预计；
- 用户接受后仍可选择：只改预计不改日期、轻微优化、重新规划或保持全部不变；
- 不评价正确率、掌握程度或学习质量；
- 每日上限只能在排期遇到困难时提出增加建议，不能自动修改。

## 5. v0.8.4 补齐

- 当日复盘使用“原计划任务”和“当天真实执行任务”的并集，提前完成、补做逾期任务和跨日计时不会消失。
- 原计划外执行在图表中的计划值为 0，不再被错误显示成当天原计划。
- 保存复盘时记录任务 ID、完成 ID、执行 ID、未完成 ID、实际和推断分钟。
- 打开过去复盘时优先使用保存快照；任务之后被重开、改期或继续执行，不会改写当日汇总历史。
- 时长建议只显示当天涉及的任务组。
- 自动复盘只在今天全部任务完成时触发，不会浏览历史日期时误弹。

---

# 三、目标系统

## 1. 最终架构

```text
Goal
├─ 期望完成日期（软约束，可留空）
├─ 最晚完成日期（硬目标）
├─ 目标优先级
├─ 一个或多个任务组完成条件
│  ├─ 全部
│  ├─ 百分比
│  └─ 数量
└─ 特殊单项任务关联
```

不存在独立 Milestone。老师检查、阶段检查和部分完成要求都用 Goal 表达。

## 2. 多目标规则

- 一个任务组可属于多个目标；
- 同一任务不会在全局统计中重复计数；
- 排期先看最近的有效期限，不因关联目标更多而增加权重；
- 期限相同才使用目标优先级打破平局；
- 较晚的完整目标不能覆盖较早的部分完成目标。

例如：

- 8 月 15 日完成化学 50%；
- 8 月 20 日完成化学 100%。

前 50% 使用 8 月 15 日作为最近期限，后 50% 使用 8 月 20 日。

## 3. 生命周期

```text
Active
→ 条件满足后自动 Completed
→ 用户可 Archived
```

目标和任务组都保留实际完成历史。进行中目标不能直接归档，因为归档会绕过仍在生效的调度约束；需要先编辑或删除并预览影响。

## 4. v0.8.4 补齐

- 没有任何有效条件或直接任务的空目标不会自动完成，进度保持 0。
- 目标完成日期来自满足条件的任务实际完成时间，而不是系统检查目标时的当前时间。
- 目标页面显示实际完成日期、是否达到期望日期、是否满足最晚日期。
- 任务组完成日期同样来自最后一个实际完成任务。
- 进行中目标的归档按钮禁用；删除目标仍进入“保持排期/利用释放空间减负”的预览。

---

# 四、验证结果

- TypeScript 严格静态检查：通过。
- v0.8 自动需求验证：53 / 53。
- 三大系统与核心场景验证：18 / 18。
- 规模验证：20 个目标、50 个任务组、500 项任务、30 个日期约束、10 个本地版本。
- 方案计算仍支持 Web Worker 取消，不先修改正式计划。

## 生产构建说明

当前容器没有安装 `node_modules`，`npm run build` 因缺少 React、Vite、date-fns、Supabase 等真实依赖类型而失败。该结果不能被宣称为生产构建通过。`npm run typecheck` 使用项目自带检查桩并已通过；部署环境仍需正常执行 `npm install` 和 `npm run build`。

---

# 五、仍按约定不属于 v0.8 的内容

- AI 自然语言交互界面；
- 学习质量、正确率和掌握度判断；
- 正式 Cancelled 任务生命周期；
- 完整依赖图和自动拆分；
- 大型计划版本历史的完整云端同步；
- 全面改造成工作/健身等通用领域 UI。

这些不是遗漏，而是此前明确延期。
