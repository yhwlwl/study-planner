# Study Planner v0.8.8 场景架构验证

- 通过：36 / 36
- 生成时间：2026-08-01T07:01:01.386Z

- ✅ **核心目标从 8/8 提前到 8/6**：目标收紧进入目标导向推荐预览；候选窗口从当前可调整未来开始。
- ✅ **化学组从核心目标放宽到 8/20**：目标放宽默认预览“保持当前排期”，优化只是用户可选方案。
- ✅ **任务组发现缺少子任务**：只创建缺少的 Assignment，并以 Insert 事件进入预览。
- ✅ **觉得当前计划太累**：先询问用户希望得到的具体减负结果，再生成推荐与备选。
- ✅ **8/10–8/15 出去玩**：范围约束先列出影响，再推荐旅行前后平衡方案。
- ✅ **新增任务组**：新组生成草稿后必须预览，也可由用户明确保留为未安排。
- ✅ **老师 8/15 检查若干任务组**：用 Goal 数量/百分比条件表达阶段检查，不新增 Milestone。
- ✅ **预计时长变化引发计划调整**：先预览预计变化；日期仍合法时保持不动，新增冲突才最小修复。
- ✅ **复盘逐项顺延不再二次重排**：用户已选日期只做精确校验；原复盘中的重复“完整调整方案”入口已移除。
- ✅ **复盘完成页提供三条明确路径**：当前方案可直接校验提交；只有用户主动获取更多方案时才进入完整方案比较。
- ✅ **已完成历史不再生成待处理冲突**：已完成超载、已完成超次数和历史数字变化不会单独阻止方案；新增任务仍按历史负载校验。
- ✅ **冲突处理选项按用途分层**：选项数量可以因问题类型不同而变化，但不再把换日、修改条件和撤销操作混为同一层。
- ✅ **复盘冲突只处理冲突项**：合法项被固定，调度器只能处理精确预览中识别出的冲突任务。
- ✅ **批量移动按用户指定结果校验**：批量移动不再无条件重算全部日期；仅冲突项需要替代方案。
- ✅ **系统改动全部先预览**：直接提交、推荐调整和探索优化均经过统一预览确认。
- ✅ **多方案由用户决定但默认界面简洁**：预先计算推荐与备选，默认只展开推荐，用户可主动比较。
- ✅ **摘要计数可逐层展开到具体问题**：第一层显示数量，点击进入具体问题，再展开涉及任务和完整原因。
- ✅ **日期负载增减使用红绿方向提示**：负载增加为红色向上，减少为绿色向下，无变化为中性。
- ✅ **选中态在手机和桌面都清晰**：选择状态同时使用明显边框、背景、阴影和“已选择”标记。
- ✅ **移动端计划调整不产生大块空白**：复杂弹窗在手机上使用 100dvh 弹性全屏结构，正文独立滚动。
- ✅ **手机首页不再被桌面 flex-basis 撑出大块空白**：Today 头部在手机上改为显式单列 Grid，内容高度由真实内容决定。
- ✅ **硬冲突逐项展示而不是只给总数**：冲突摘要可展开到类别、数值、任务、后果和处理方式。
- ✅ **有冲突时主按钮仍提供下一步**：按钮按状态引导处理冲突或重新计算，不因 infeasible 简单全部禁用。
- ✅ **一次性例外支持部分接受与部分拒绝**：每项例外独立决定；拒绝项释放任务，接受项进入新计算条件。
- ✅ **一次性例外保持最小任务授权**：日期、规则、临时上限和涉及任务共同限定例外，不修改永久设置。
- ✅ **无可行候选时仍提供可点击解决路径**：没有候选时不只留下灰色按钮，而是给出扩大搜索、改容量和改目标入口。
- ✅ **移动端月历完整保留七列**：移动端取消强制宽度和横向空白，七列在视口内完整呈现。
- ✅ **日期容量增加不被当作必须修复**：新增容量默认保持排期，用户可主动选择减负或提前。
- ✅ **Today 任务卡不再绕过统一移动校验**：任务移动通过精确预览或调度方案，不由快捷按钮静默执行。
- ✅ **缓冲日长任务与高强度任务被明确识别**：分析器只把缓冲日中仍未完成的长任务和高强度任务列为问题；已完成历史不参与重排。
- ✅ **目标没有有效完成条件时不会自动完成**：空目标保持进行中且进度为 0。
- ✅ **目标完成历史记录实际达成日期与按期结果**：完成时间来自实际执行记录，并保存期望/最晚日期结果。
- ✅ **复盘同时纳入原计划与当日真实执行**：跨日补做、提前执行和计划外执行不会从复盘消失。
- ✅ **方案内逐项微调重新完整验算**：用户在预览中修改日期或锁定结果后，重新计算容量、上限、目标和保护日期。
- ✅ **事件协调器按意图完整度运行分流**：review=validate-and-commit；move=validate-and-commit；relax=optional-optimization；insert=recommended-preview；tired=exploratory-optimization
- ✅ **部分目标与后续完整目标不会互相覆盖**：前 50% 最近期限=2026-08-15；后 50% 最近期限=2026-08-20
