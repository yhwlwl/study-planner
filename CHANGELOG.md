# Changelog

## v0.6.4

- 手动拖拽、批量移动、整体顺延、局部修复和全面重排后，自动检测同一任务组的日期顺序是否与编号顺序冲突。
- 检测到“01 排在 02 后面”等情况时弹出重新编号建议，不会擅自修改任务名称。
- 支持按任务组勾选，只对选中的组按当前计划日期重新编号。
- 重新编号只修改任务的序号与显示标题，不移动日期，不改变进度、实际用时、计时记录、备注、锁定和手动安排保护。
- 同一天内维持原编号先后，未安排任务排在已安排任务之后；重复任务不参与重新编号。
- 弹窗提供完整前后预览，也可直接选择“保留原编号”。

## v0.6.3

- 数字输入默认仅接受非负整数，禁止小数、负号、科学计数法和其他字符。
- 手机端数字输入改用 `inputMode="numeric"` 的文本草稿，减少浏览器数字框允许 `-`、`.`、`e` 的差异。
- 支持粘贴校验和全角数字转换；非法内容不会写入任务或设置状态。
- 超出字段最小值或最大值时，在离开输入框时自动调整到合法边界并明确提示。
- 为任务数量、单次时长、每日上限、每日容量、计时结算等字段补充合理最大值。
- 保留聚焦自动全选、可暂时清空、编辑完成后再提交等 v0.6.2 性能优化。

## 0.6.2

- 修复自动保存与 Supabase 同步在计划数据较大、重排历史较多时明显变慢的问题。
- 云端同步不再上传重排前后完整快照和冲突备份；这些大体积恢复数据改为仅保存在当前设备，本体计划、任务、日期配置、计时和统计记录仍完整同步。
- 本地 IndexedDB 将活动计划、重排历史和冲突备份分开存储；普通任务修改只重写活动计划，不再每次重复写入数 MB 历史快照。
- 本地连续保存改为“只保留最新状态”的串行队列，避免快速操作产生过时写入积压。
- 云端连续保存改为串行合并队列：同一时间只发出一个 Supabase upsert，新修改自动合并为下一次最新快照，避免并发请求和旧版本后完成覆盖新版本。
- 云同步防抖由 1200 ms 调整为 450 ms，并区分“已保存到本机 · 等待云同步”和“正在同步到云端”，不再把等待时间误显示成网络请求耗时。
- 登录恢复时并行读取本机缓存和云端快照，并复用已知用户 ID，减少重复会话查询。
- 切到后台或关闭页面前会尽快提交已排队的云端保存，降低浏览器后台定时器节流造成的延迟。
- 修复冲突备份把完整历史再次嵌套保存、导致数据体积持续膨胀的问题；旧备份会在载入时自动压缩为最多 3 份不含嵌套历史的便携快照。
- 普通状态修改不再深拷贝重排历史字符串；整体顺延、日历预览与重排候选也只克隆活动计划，减少手机端主线程卡顿。
- 设置页数字和计划名称改为编辑完成后再提交，避免输入每一位数字或每个字符都触发一次本地与云端保存。

## 0.6.1

- 统一重做全部数字输入框的手机输入体验：获得焦点时自动全选当前值，直接输入即可覆盖默认的 0、1、30 等数字。
- 数字输入框允许在编辑过程中暂时清空，不再因为受控状态立即把旧值或 0 填回来。
- 失去焦点时才进行最小值、最大值校验和纠正，避免输入多位数时被中途截断。
- “每日最多数量”不再默认显示容易产生 `02` 的 0；留空表示使用任务活动类型的默认上限，并显示明确占位提示。
- 修复范围覆盖任务新增/编辑、重排设置、今日额外时间、活动日容量、完成进度、计时结算、整体顺延和系统设置中的全部数字字段。

## 0.6.0

- 重构重排引擎为基于真实执行状态的滚动规划：过去日期冻结、今天半冻结，未来日期才是主要优化范围。
- 重排会把真实计时、手动补录和无实际时间的已完成任务推定用时全部计入精力负载；不计入正式统计的任务仍占用排期容量。
- 全面重排默认从明天开始；重排前显示今日执行快照，可明确选择今天不再新增、还能学 30/60 分钟或自定义时间。
- 已完成任务继续计入真实完成日的同类任务数量，避免把未来文言文、背诵等错误搬到已经完成多次的今天。
- 新增统一每日硬限制：文言文学习 4 次、文言文默写 1 篇、正式背诵 1 次、化学预习课 1 节、数学整套试卷 1 套、长任务常规日 1 个/学习日 2 个、高强度任务 2 个。
- 所有每日上限都支持在冲突预览中“仅本次放宽”，不会永久修改任务组默认规则；没有合法位置时明确返回待决定，不再强塞。
- 局部修复优先搜索问题日前后 3 天，并对来源日、目标日、真实负载、同类数量、截止日期和用户意图进行完整验算；附近无解才扩大范围。
- 最少改动方案只处理真实冲突和未安排任务，不再为了“全面”而无谓重排所有未来任务。
- 新增“休息缓冲”候选方案，在滚动 7 天内保留轻量位置，并避免连续高负载和高强度任务堆叠。
- 自动缓冲日会先验算剩余工作量与目标利用率余量；余量不足时宁可不新增缓冲日，也不会把其余日期压满或制造无处可排任务。
- 日期详情可手动设置完全休息或自定义可用分钟、活动原因和后续调整偏好；手动缓冲日默认永久受保护。
- 完全休息时优先把原日任务包整体顺延；只能学习部分时间时优先保留重要短任务，仅移出必要任务，并控制后续每日负载变化。
- 重排中心新增受保护缓冲日控制，可明确选择“仅本次允许使用”，且仍遵守该日容量与任务上限。
- 方案对比新增改动日期、原计划保留率、平均负载变化及候选日期被排除原因，增强可解释性。
- 日历单项和批量手动移动现在会检查全部同类上限、长任务、高强度、容量和截止风险，并在继续前说明后果。
- 移除锁定按钮旁容易误触的“重新打开”图标；已完成任务只能从更多菜单进入，且必须二次确认，实际学习时间不会删除。
- 游客默认演示计划升级为约 29 天、约 190 个任务实例的完整规模示例，内容使用通用演示素材；游客仍可一键切换为空白计划。

## 0.5.0

- 将统计页升级为“概览、趋势、科目、执行质量”四个页签，重点增强图表和可钻取数据。
- 新增今日、近 7 天、本周、全部和自定义统计范围，并记住上次选择。
- 新增任务完成率与时间加权工作量完成率双口径，部分完成按当前进度折算。
- 修复计划时间与实际时间统计口径不一致的问题；默认不计入统计的任务，其实际时间单列为“额外学习”。
- 实际时间趋势按真实计时记录日期归属；旧数据缺少记录日期时回退到完成日期或计划日期。
- 新增学习热力图，可切换学习时间和工作量完成率，点击日期查看当天任务。
- 每日趋势改为实际学习柱形、计划线和 7 日移动平均，并支持图表/表格切换与全屏查看。
- 新增各科投入横向排名、任务组展开、科目完成进度和预计时长准确度分析。
- 新增按期完成率、延期任务、顺延任务、计划变更率和每日执行轨迹。
- 新增专注总时长、有效专注次数、平均时长、最长专注及每日专注趋势。
- 新增规则生成的本周洞察，可从执行偏差直接进入局部修复预览。
- 从本版本开始记录时间来源，区分沉浸式计时、手动补录和旧版未知来源。
- 手机端优化为紧凑指标卡、横向热力图、全屏图表和底部日期详情。

## 0.4.1

- 修复切换浏览器标签页后，Supabase 重复触发同一账号会话事件，导致页面永久停留在“正在安全切换数据空间”的问题。
- 同一用户的令牌刷新、会话恢复和重复登录事件现在只更新会话信息，不再重新切换数据空间。
- 退出登录的数据切换加入 `finally` 收尾，即使本地缓存清理异常也不会永久卡住隐私遮罩。

## 0.4.0

- 新增独立的沉浸式计时页面；从今日任务点击“开始计时”后立即进入纯净计时界面。
- 计时页隐藏侧边栏和普通页面信息，只保留任务名、科目、超大计时、预计时长进度环与核心操作。
- 支持暂停、继续、退出专注和结束计时；退出专注不会结束或清空计时。
- 结束计时时可选择仅记入时间、保存为部分完成或直接标记完成，并可修改最终记入分钟数。
- 支持空格暂停/继续、Enter 结束计时、Esc 退出专注。
- 浏览器标签标题会同步显示当前计时和任务名称。
- 离开沉浸页后，顶部显示实时计时入口，可随时返回。
- 同一时间只允许保留一个计时任务；其他任务会引导返回当前计时，避免切换时丢失记录。

## 0.3.0

- 月历中的“+N 项”改为独立快捷面板：桌面端悬浮、手机端底部抽屉，只展示被折叠任务。
- 被折叠任务可直接拖到其他日期，并支持多选、批量移动、顺延一天和锁定。
- 日期格任务按正在计时、用户手动、锁定、优先级和时长排序，并按屏幕宽度动态展示 2—4 项。
- 拖动到目标日期时实时显示放入后的负载；超载、越过目标日期或违反每日上限时先说明后果。
- 点击具体月历任务打开任务快捷抽屉，可改期和锁定；点击日期空白仍打开原有全天管理面板。
- 重排中心“修改前后负载”支持逐日展开双栏对比：新增为绿色，移除为红色删除线，相同任务对齐。
- 双栏对比中可直接接受、保留原日、改到指定日期、锁定结果，并切换“全部任务 / 只看变化”。
- “逐项微调”改为按日期展开，最多同时展开 3 天；可把修改后任务拖到另一日期。
- 用户在预览中亲自调整的任务标记为“本次预览固定”，重新计算时仍保留，也可解除固定。
- 新增预览内逐步撤销；日期负载即时刷新，复杂目标风险延迟约半秒刷新。
- 逐日详细调整使用右侧抽屉，可直接修改日期类型和自定义容量并查看超载变化。
- 重排历史新增重排后快照与人工决策记录，可查看完整的日期任务前后差异。

## 0.2.1

- 新增“整体顺延”入口，可选择仅顺延当天未完成任务，或从所选日期起整体顺延未来全部未完成任务。
- 整体顺延支持 1—14 天、是否包含锁定任务，并在应用前展示任务变更、截止日挤压和负载风险。
- 每日重复任务不会被整体顺延，避免单词等日常任务错位。
- 整体顺延会记录为用户手动意图，后续自动重排优先尊重。
- 修复选择“最少改动”后点击“重新计算”却自动切回“均衡负载”的问题；当前方案会持续保留。

## 0.2.0

- 新增局部修复与全面重排。
- 新增最少改动、均衡负载、优先保目标三种方案。
- 新增逐项接受、保持、改选日期和锁定。
- 手动移动记录为用户意图，近期禁止拉回原日期。
- 新增近期冻结、软超载、每日任务数量、科目占比和连续高负载规则。
- 记忆任务默认分散，同一父任务每天不超过 2 项。
- 新增日期类型变更的预览流程。
- 新增结束今天和跨日未完成自动提示。
- 新增重排前后负载对比及微调后风险复检。
- 新增最近 10 次重排历史。
- 游客演示与登录账号数据空间隔离。
- 登录/退出切换时显示隐私遮罩。
- 移除公开源码中的个人计划种子和旧 standalone 版本。
- 新增任务的拆分允许、记忆任务属性。

## 0.1.1

- 登录后自动从云端恢复并自动保存。
- 修复月历底部负载与任务数量重叠。

## 0.6.5

- 新增服务端访问日志：记录服务端时间、IP、IP 推断国家/地区/城市、时区、路径、浏览器、屏幕与应用版本。
- 访问日志通过 Vercel Function 写入受保护的 Supabase `visit_logs` 表，前端 anon/authenticated 角色无权读写。
- 访问日志失败时静默跳过，不影响计划加载、离线使用、计时或云同步。
- 新账号云端为空时仍由用户选择起点；完整演示计划调整为推荐选项，绝不自动上传游客本地数据。
