# 更新日志

## v0.8.12 · 2026-08-01

- 重构手机端“计划调整预览”首屏，去除重复的处理方式和用户操作优先说明。
- 单一局部操作直接展示本次结果，不再显示唯一推荐方案卡。
- 方案底部操作区从滚动正文中拆出，避免按钮遮挡统计与详情。
- 主按钮按事件显示具体动作，例如“保存任务组调整”。
- “生成更多不同方案”改为次级“查看其他方案”入口。
- 修复 `modal-wide` 的 90vh 规则覆盖手机全屏高度，最终保持 100dvh。
- 新增 390×844 与 1440×900 浏览器渲染验证。

## v0.8.11 · 2026-08-01

- 修复手机端复盘未完成任务卡仍使用桌面双栏，导致标题逐字竖排和选择框覆盖。
- 将手机任务卡最终布局固定为纵向单列，并约束标题、进度条和选择框宽度。
- 新增 390×844 真实浏览器级联渲染验证。

## v0.8.10 · 2026-08-01

- 活动计时任务不能从 Today 完成入口绕过计时结算；增加累计分钟兜底。
- 每日统计抽取为独立纯函数，旧版实际时间残差只计算一次。
- Today 区分原计划、实际、剩余和执行负载，过去／未来日期使用对应文案。
- “全部任务”重构为任务收件箱和任务组双视图，覆盖未安排、逾期和复盘后待处理任务。
- 批量顺延只保留统一预览一个最终确认点；删除入口移除重复前置确认。
- 批量移动改为日期选择器和任务摘要，不再使用浏览器 prompt。
- JSON 导入、云端恢复和冲突备份恢复统一显示覆盖差异。
- 任务标题与日期备注使用草稿一次保存；时长修改按最小作用域预览。
- 算法参数、恢复维护、任务组高级规则和目标特殊关联默认折叠。
- 应用方案后提供就近撤销，长表单不再因误点背景关闭。
- 综合验证 76/76、场景 44/44、UI 25/25、效率 22/22、500 项规模测试通过。

## v0.8.9 · 2026-08-01

- 明确局部操作的第一方案固定为“只执行用户操作”。
- 单任务和任务组删除改为准备态预览，不再直接提交或自动触发全局重排。
- 新增 `assignment-deletion` 事件和三层作用域：用户操作、直接影响、未来优化。
- 比较操作前后硬约束事实，区分既有、已解决、已缓解、新增或恶化问题。
- 既有问题不再阻止删除等局部操作；只有本次新增或恶化问题需要处理。
- 用户主动获取更多方案时才把局部事件提升为优化动作。
- 桌面与手机方案预览新增作用范围摘要。
- 新增局部删除运行测试、40 项场景验证和 17 项 UI 审计。

## v0.8.8 · 2026-07-31

- 为复盘、计划调整入口和方案预览增加独立桌面弹窗规格，不再用同一个 840px 宽度承载所有高密度页面。
- 复杂弹窗在桌面端采用受控最大宽度、固定头部、独立正文滚动和粘性底部操作区；手机端继续使用 100dvh 全屏与安全区。
- 复盘摘要从六张过窄卡片改为桌面三列两行、手机两列；压缩装饰区，强化“摘要—明细—最终决定”层级。
- 计划调整入口改为桌面主次双栏：左侧表达变化与目标，右侧设置今天可接收容量和保护规则；手机端自动回落单列。
- 计划调整中的“今天还能接收多少任务”从超宽分段条改为清晰的 2×2 选项卡，避免大面积空白。
- 方案预览改为自适应方案卡、桌面四列摘要、手机两列摘要，详情继续渐进展开。
- 统一目标、统计、设置、任务组、日期约束、历史差异和表单控件的卡片层级、圆角、阴影、间距与桌面/手机重排。
- 新增 15 项桌面与手机 UI 静态审计；综合验证 72/72、核心场景 36/36、运行级调整测试和 500 项规模测试通过。

## v0.8.7 · 2026-07-31

- 复盘完成页改为三条路径：按当前方案直接顺延、主动获取更多方案、仅保存复盘暂不顺延。
- 当前复盘方案只做精确完整校验；合法时直接提交，不再运行完整多方案调度。
- 已完成任务和实际用时从待处理硬冲突中排除；已完成 9 小时、5 套卷子等历史事实不会阻止应用。
- 历史执行仍计入当天真实负载和规则计数，新的未完成任务继续基于历史负载接受校验。
- 新旧冲突改为结构化语义比较；既有超载从 609 分钟减少到 594 分钟不会因提示数字变化被误判为新冲突。
- 冲突选项按“处理当前任务 / 修改产生冲突的条件 / 不继续这部分调整”分层，减少平级近义选项。
- 综合验证 71/71，核心场景 36/36，复盘与已完成历史运行测试、部分例外测试和 500 项规模测试通过。

## v0.8.6 · 2026-07-31

- 修复手机 Today 首页因桌面 `flex-basis` 在纵向布局中变成高度而产生的大块空白，改为明确的响应式 Grid。
- 新增硬冲突分类：绝对阻断、用户保护、可一次性例外、目标/结构冲突和提醒。
- 硬冲突可逐项查看涉及任务、当前值、允许值、后果和解决方式，不再只有总数。
- 支持对多个冲突分别接受、拒绝、保留原安排、解锁、留为未安排或返回修改目标/容量。
- 用户部分接受例外后，从当前选中候选继续计算；保留已接受部分，只释放被拒绝部分，并再次预览。
- 一次性例外收紧到日期、原始规则、临时上限和涉及任务；同规则多任务授权会合并任务范围而非扩大为整日权限。
- 冲突时主按钮改为处理问题或按选择重新计算；无候选时提供扩大范围、修改容量和修改目标入口。
- 综合验证 68/68，核心场景 33/33，部分接受例外运行测试和 500 项任务规模测试通过。

## v0.8.5 · 2026-07-31

- 新增变化驱动的计划调整协调器，按用户意图完整度分流为精确校验、推荐预览、可选优化和探索式优化。
- 复盘逐项顺延不再把用户已选日期清空后二次重排；合法项固定，冲突时只处理冲突项。
- 批量移动同样按用户指定结果精确校验，不再无条件生成全局重排。
- 目标放宽和容量增加默认保持当前日期；减负或提前只作为用户可选方案。
- 推荐方案默认展示，系统同时准备实质不同的备选；更多方案由用户主动展开。
- 方案页改为摘要优先、计数可展开、项目可继续查看完整原因和计算依据。
- 日期负载增减使用红绿方向提示，容量变化单独展示；选中态同时使用边框、背景、阴影和文字标记。
- 修复手机复杂弹窗的大块空白与滚动结构，保留完整七列月历、安全区和完整换行。
- 自动验证 62/62，核心场景 27/27，复盘精确校验运行测试和 500 项任务规模测试通过。

## v0.8.4 · 2026-07-31

- 再次按完整对话复核计划调整、复盘与目标三大系统。
- 方案支持逐项保留原日、自定义日期和锁定结果，并重新验算全部约束与目标影响。
- 稳定性评分补齐移动距离、负载、目标风险和保护日期因素。
- 复盘纳入原计划与真实执行任务，并保存历史快照，避免后续重开或改期重写过去复盘。
- 目标和任务组使用真实任务完成时间；空目标不自动完成；进行中目标不能直接归档。
- 自动验证 53/53，核心场景 18/18，500 项任务规模验证通过。

## v0.8.3 · 2026-07-31

### v0.7.0 以来全量需求回归与行为闭环

- 按完整对话而不只按冻结 MD 重新审计数据模型、目标、创建、日期约束、调度、方案、复盘、版本、游客数据和移动端。
- 计划调整入口改为“实际发生了什么 → 取舍偏好 → 方案”，删除旧 ReplanDialog 用户模型。
- 补齐“计划太累”显式事件、更多实质不同方案、非日期结构变化前后对比和任务换组确认。
- 已完成任务的每日规则计数优先使用实际完成日期；正在计时和历史执行继续冻结。
- 缓冲日明确禁止长任务和高强度任务，分析器会把现存违规列为危险问题。
- 日期范围或单日容量放宽改为可选优化，不再自动提前任务；限制收紧仍进入修复。
- 移除 Today 任务卡中绕过完整校验的前后移动按钮。
- 任务组减量只允许删除安全的连续尾部任务；自定义标题、预计时长、实际记录和锁定状态得到保护。
- 复盘自动弹出只针对今天全部完成；摘要卡可展开任务和七日图表；时长建议只显示当天相关任务组。
- 接受新预计时长后，“日期保持不变”会真正保存新预计；历史实际记录不改变。
- 更新并运行 53 项自动验证、12 项场景验证及 500 任务规模验证。


## v0.8.2 · 2026-07-30

### 场景架构纠偏与复盘融合

- 修正变化事件日期被错误用作调度搜索起点的问题：目标提前、旅行范围和新任务现在可使用当前未来窗口中的合法日期。
- 修正百分比/数量 Goal 把整个任务组都提前的问题；只约束满足条件所需的 Assignment。
- 修正 Goal 变更事件候选范围，使用旧/新条件实际计数任务的并集，不再把无关的整组任务交给移动器。
- 增加 Goal 优先级；排期仍先比较最近相关期限，仅在同期限时用优先级打破平局。
- 将“计划太累”升级为显式 `load-preference-change` 事件，内部采用 `optimize + rest`。
- 合并“结束今天”和“结束学习并复盘”，在一个页面完成复盘、未完成任务处理、完整调整入口和时长建议。
- 重构复盘视觉：完成率摘要、计划/实际对比、决策卡、时长建议卡，以及默认折叠的六类图表。
- 修复桌面 Today 日期单字换行和操作按钮零散换行。
- 将手机端完成任务更多按钮替换为固定横向 `Ellipsis` SVG。
- 新增九项场景架构验证，自动验证扩展为 53/53。

## v0.8.1 · 2026-07-30

### 计划调整中心融合修订

- 将“解决当前问题 / 重新组织未来计划”从首屏算法选择移入折叠的调整范围设置。
- 首屏改为“发生了什么 + 当前方案是否仍有非法位置”的事件结果摘要。
- 保留四种取舍方案，但明确其只是少改、均衡、目标和休息之间的偏好，不再与调整范围混为一谈。
- 将问题数、任务移动、日期影响和目标预计统一为一组可点击指标；明细只在一个位置展开。
- 删除“系统检测问题 → 指标 → 方案后果 → 同样文字摘要”的重复链路。
- 合并“修改前后负载”和“逐项微调”，同一个日期列表同时承担负载对比、任务差异、接受/保留/改期和拖动微调。
- 合并“按当前微调结果检查”和“仍需注意”，只基于当前预览展示一次；硬风险展开，普通提醒折叠。
- 将应用按钮改为“应用这个方案”，关闭按钮改为“不应用，关闭”，减少旧“重排”术语。
- 保留完整的任务原因、影响与“为什么没有选择其他日期”解释。

## v0.8.0 · 2026-07-30

### 架构

- 新增 Goal、GoalCondition、CalendarConstraint、PlanChangeEvent、SchedulingProposal 和 PlanVersion。
- 移除当前产品中的独立 Milestone 概念。
- 旧全局目标日期仅用于 v0.7 数据迁移，当前调度统一读取 Goals。
- Insert、Repair、Optimize、Rebuild 复用一个约束与候选核心。

### 创建与目标

- Today 移除浮动加号，改为标题旁低干扰“添加任务”。
- 全部任务分离“添加单项任务”和“创建任务组”。
- 单项任务支持归组、独立、系统排期、偏好日期和锁定日期。
- 新增 Goals 顶级页面和三类完成条件。
- 一个任务组可参与多个目标，多目标全局统计不重复计数。

### 计划调整

- 所有智能变化先生成方案，不静默应用。
- 方案显示任务、日期、负载、目标、问题、手动意图、例外和拒绝日期原因。
- 更多方案按移动集合、日期、负载、目标结果和保护日期使用去重。
- 方案计算移入可取消 Web Worker。

### 复盘、版本和统计

- 复盘默认摘要优先，增加可展开的任务、任务组、时间、完成率、误差和平均时长图表。
- 自适应时长使用最近 10 个有效样本、最小样本和 IQR 异常值处理。
- 重大变化创建本机计划版本；恢复前保存当前计划并保护执行记录。
- 统计新增当前计划/历史计划视角和目标风险。

### 数据、移动端和性能

- v0.7 状态确定性迁移到 schema 8。
- 云端当前状态与本地重型版本历史分离。
- 修改游客计划在注册/首次登录时明确询问是否导入。
- 保留 iPhone standalone、安全区、100dvh、七列月历和完整换行。
- 新增 44 项自动验证和 500 任务规模夹具。
