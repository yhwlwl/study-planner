# Study Planner v0.7.0 → v0.8.8 迁移指南

## 1. 迁移原则

v0.8.8 在首次读取旧状态时执行确定性迁移。迁移只转换数据结构，不主动重排任务，不删除实际用时、完成记录、部分完成进度、锁定状态、手动排期意图或备注。

建议升级前先在 v0.7.0 设置页导出 JSON 备份。升级后也应再次导出 v0.8.8 备份，作为迁移后的独立恢复点。

## 2. 架构版本

- v0.7.0 旧状态会被识别并归一化为 `SCHEMA_VERSION = 9`。
- 已经是 v0.8.x 的状态仍会经过无损规范化，用于清理悬空链接、补齐缺省字段和修正任务组数量。
- 迁移失败时不会覆盖原本可用的本地状态。

## 3. 旧目标日期迁移

旧设置中的全局核心目标日期、化学目标日期和任务组目标/截止字段不再作为当前可编辑真相。

迁移规则：

1. 旧核心目标日期生成一个标记为“由旧版迁移”的目标。
2. 旧化学目标日期生成一个对应化学任务组的迁移目标。
3. 任务组自身存在独立截止含义时，生成或并入对应目标，避免重复。
4. 迁移目标使用“全部完成”条件，以尽量保留旧版含义。
5. 迁移完成后，设置页不再出现旧全局目标日期输入；当前调度只读取 Goals。
6. 兼容字段仅保留在迁移读取层，不参与 v0.8.8 当前调度和统计。

## 4. 任务与任务组迁移

每个 Assignment 会补齐：

- `titleCustomized`：识别自定义标题，防止重命名或重编号覆盖。
- `durationCustomized`：识别单项自定义预计时长。
- `scheduleSource` 与 `intentStrength`：保留手动安排和锁定意图。
- `standalone`、`createdAt`、`updatedAt`、`createdBy`。
- 旧状态、进度、实际分钟和时间记录的规范化结果。

任务组数量以实际 Assignment 数量为准，不再允许“界面显示 15 项、实际只有 13 条记录”的双重真相。

独立任务使用隐藏的单项任务组作为内部实现，但普通界面不会展示该技术对象；删除最后一个独立任务后会安全清理孤儿组。

## 5. 日期可用性迁移

旧活动日、旅行日、完全休息、降低容量、缓冲日和保护信息会转换为统一 `CalendarConstraint` 或兼容的基础日类型。

- 新的日期可用性和保护统一在设置页“日期可用性与保护”管理。
- 日期范围只保存一条约束，不为每一天重复创建互相冲突的规则。
- 移除约束不会自动恢复过时的旧排期，而是生成候选方案。

## 6. 重排历史与计划版本

- 旧重排历史尽量继续可读、可恢复。
- v0.8.8 的重大变化改用 `PlanVersion`。
- 本地版本、旧重排历史和冲突备份不会被递归嵌入新快照。
- 云端只同步当前可移植状态；完整计划版本仍为设备本地数据。
- 恢复旧版本前，系统先把当前状态保存为新的可恢复版本。
- 恢复时，当前实际用时、时间条目、完成状态和完成日期按保护策略覆盖旧快照中的过时执行信息。

## 7. 游客与账号

旧游客计划继续存放在游客命名空间。

首次登录且账号云端没有计划时：

- 若游客计划被修改，必须选择是否导入。
- 导入会复制任务、任务组、目标、日期约束和当前执行状态到账号空间。
- 选择不导入时，账号和游客数据继续完全分离。
- 系统不会把修改后的游客状态静默上传到账号。

## 8. 升级后检查清单

1. 打开“目标”，确认迁移目标的日期和任务组范围。
2. 检查已完成、部分完成、正在计时和锁定任务是否保持原状态。
3. 打开月历，抽查手动移动过的任务日期。
4. 打开设置中的日期约束，确认旅行、休息和缓冲范围。
5. 打开统计，确认实际用时没有减少。
6. 在设置中查看本机计划版本提示。
7. 新建一个单项任务，确认系统先显示方案而不是直接改写日历。
8. 导出 v0.8.8 JSON 备份。

## 9. 回滚

v0.8.8 导出的状态包含 v0.8 新实体，不能保证 v0.7.0 完整识别。需要回滚时，应使用升级前保存的 v0.7.0 JSON 备份和原 v0.7.0 程序，而不是把 v0.8 数据强行导入旧版。
