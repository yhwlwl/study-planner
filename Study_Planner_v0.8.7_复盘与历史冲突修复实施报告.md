# Study Planner v0.8.7 复盘与历史冲突修复实施报告

## 结论

本轮已经完成对话中最新确认的两项核心纠偏：

1. 复盘中用户已经选择好的顺延方案可直接校验并提交，完整多方案重排只在用户主动请求时运行；
2. 已经完成的超容量、超次数任务作为不可变历史保留，不再被列为待处理硬冲突，也不再阻止应用其他变化。

## 已实现内容

### 复盘流程

- 新增“完成复盘，并按当前方案顺延 X 项”；
- 新增“获取更多方案”；
- 保留“仅保存复盘，暂不顺延”；
- 当前方案合法时直接原子提交；
- 当前方案有冲突时只展示精确方案中的冲突，不自动生成其他方案；
- 用户主动获取更多方案时，才进入完整方案比较。

### 历史与冲突边界

- 已完成任务和实际用时计入历史负载，但不作为可移动冲突项；
- 已完成 5 套卷子、9 小时等情况不会单独出现硬冲突卡片；
- 当天仍有未完成或新加入任务时，只针对这些任务进行容量和每日上限校验；
- 方案只要没有新增或恶化已有超载，就不会被旧历史冲突阻断；
- 冲突信息明确说明历史事实保留、只处理仍可调整任务。

### 冲突界面

- 处理当前任务、修改产生冲突的条件、撤销相关调整分区展示；
- 操作名称改为具体结果，减少含义重叠；
- 保留逐项接受/拒绝、带条件重算和二次预览。

## 变更文件

- `src/lib/planner.ts`
- `src/lib/conflicts.ts`
- `src/components/ReviewDialog.tsx`
- `src/components/ProposalDialog.tsx`
- `src/App.tsx`
- `src/styles.css`
- `scripts/runtime-adjustment-v087.mjs`
- `scripts/scenario-v087.mjs`
- `scripts/validate-v08.mjs`
- 发布和追踪文档

## 自动验证

| 项目 | 结果 |
|---|---:|
| TypeScript 严格检查 | 通过 |
| 综合验证 | 71 / 71 |
| 核心场景验证 | 36 / 36 |
| 复盘与已完成历史运行验证 | 通过 |
| 部分接受/拒绝例外运行验证 | 通过 |
| 20 Goal / 50 组 / 500 任务规模验证 | 通过 |

## 构建边界

当前容器未安装 React、Vite、Supabase 等真实 npm 依赖，无法把真实 Vite 生产构建标记为通过。项目严格 TypeScript 检查和不依赖外部包安装的运行级验证均已通过。应在正常 npm 网络环境执行 `npm install && npm run build`。
