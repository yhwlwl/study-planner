# v0.8.6 实现状态

状态：手机首页布局修复与逐项冲突决策闭环完成，已通过静态、运行级场景和规模验证。

## 已完成

- Today 手机端改为内容驱动的单列 Grid，消除大块空白。
- 硬冲突按性质分类，并逐项展示任务、数值、后果和可选解决方式。
- 用户可分别接受或拒绝一次性例外；拒绝项释放给调度器，接受项作为本轮新条件。
- 重算从用户选中的候选继续，保留合法移动和逐项微调，再次生成完整预览。
- 一次性例外精确到日期、规则、临时上限和任务，不修改永久设置。
- 冲突状态和无候选状态均提供可点击的下一步，不再只显示灰色禁用按钮。

## 验证

- TypeScript 严格检查：通过。
- 综合验证：68 / 68。
- 核心场景：33 / 33。
- 逐项接受/拒绝例外运行测试：通过。
- 500 项任务规模测试：通过。

生产构建需要在可正常安装 React、Vite、date-fns、Supabase 等 npm 依赖的环境中执行。
