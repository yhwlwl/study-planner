# V0.2.1 实现状态

## 已实现

- 局部修复与全面重排两种模式。
- 最少改动、均衡负载、优先保目标三套方案。
- 重排前预览；逐项接受、保持、改选日期、锁定。
- 手动移动记录为用户意图，近期禁止自动退回原日期。
- 冻结近期计划，可设为 0 让系统提出调整手动任务的建议。
- 常规日/学习日软超载、每日任务数量、长任务数量规则。
- 同科目占比均衡与连续高负载缓冲提示。
- 记忆类任务分散；同一父任务默认每天不超过 2 项。
- 日期类型变化在影响任务时先生成重排预览。
- 修改前后负载对比和微调结果风险复检。
- 结束今天；跨日后自动提醒处理未完成任务。
- 整体顺延：支持仅当天或从当天起全部未完成任务，应用前预览影响。
- 重排方案选择会在重新计算后保持，不再自动切回均衡方案。
- 部分完成剩余时间、实际用时校准建议。
- 最近 10 次重排历史及恢复。
- 游客演示、不同登录账号的数据空间隔离。
- 登录后自动云端恢复、修改后自动保存、联网重试和冲突备份。
- 退出登录时隐私遮罩和本地账号缓存清理选项。
- 移除公开源码中的个人计划种子；旧云端数据可正常迁移。
- 修复月历文字重叠，并增加手动安排/锁定状态提示。

## 部分实现

- 缓冲日：排期评分会主动避免连续高负载，并在预览中提出缓冲日建议；尚未提供“一键指定为缓冲日”的独立日期类型。
- 任务拆分：数据模型和任务编辑已支持“允许拆分”属性，部分完成可记录剩余时间；尚未在重排预览中自动生成两个独立任务片段。
- 修改前后月历：已提供变化最大的日期负载对比，尚未制作完整双月历并排视图。

## 暂未实现

- 浏览器系统通知。
- 强制或完整的每日复盘表单。
- DeepSeek API（按需求仅预留）。
- 多人协作、共享同一计划。
- 自动拆分任务为多个可独立打勾的片段。

## 验证

- 严格 TypeScript 静态检查通过。
- 排期运行时回归测试通过：手动延期不回退、解冻后不退回原日、每日上限、记忆任务分散、超载识别、游客隔离。
- 旧 V0.1 数据迁移测试通过，原账号云端任务不会因移除公开种子而丢失。
- 当前执行环境无法完成 `npm install`，因此未在此环境运行真实 Vite 生产构建；Vercel 将按 `package.json` 安装依赖并构建。
