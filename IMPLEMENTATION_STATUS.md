# V0.4.1 实现状态

## 已实现

- 修复浏览器标签页切换、令牌刷新或会话恢复时重复触发同一账号事件，导致永久停留在“正在安全切换数据空间”的问题。
- 独立沉浸式计时页：点击开始计时后隐藏普通导航，仅显示任务、超大计时与核心控制。
- 计时支持暂停、继续、退出专注、结束并记录；退出页面不丢失累计时间。
- 结束计时可仅记入时间、保存为部分完成或标记完成，并可手动修正分钟数。
- 顶部实时计时入口、浏览器标题同步与键盘快捷键。
- 同时只保留一个计时任务，避免切换任务造成未记录时间丢失。
- 局部修复与全面重排，最少改动、均衡负载、优先保目标三套方案。
- 重排方案在“重新计算”后保持，不会自动跳回均衡模式。
- 用户手动安排优先保护、近期冻结、72 小时内不拉回原日期、永久锁定。
- 今日未完成任务整体顺延，以及从某日开始整体顺延未来计划。
- 月历日期格按屏幕宽度动态显示 2—4 项，其余任务通过“+N 项”快捷面板管理。
- “+N 项”桌面悬浮面板、手机底部抽屉；支持直接拖拽、多选移动、顺延和锁定。
- 点击日期空白打开全天管理；点击具体任务打开任务快捷抽屉；三种点击语义互不混淆。
- 月历拖动实时展示目标日放入后负载，并对每日上限、超载和越过目标日期进行确认。
- 重排前后负载逐日双栏对比，相同任务对齐，新增绿色，删除红色划线，信息变化黄色。
- 双栏对比中直接接受、否决、改期、锁定，并支持“只看变化”。
- 逐项微调按日期展开，最多同时比较 3 天，支持在日期之间拖动任务。
- 用户微调自动成为“本次预览固定”，重新计算保留，可手动解除。
- 预览内逐步撤销；简单负载即时刷新，复杂风险停止操作约 450 毫秒后刷新。
- 逐日详细调整右侧抽屉，可改日期类型、自定义容量并实时查看影响。
- 每次正式应用作为一个历史版本；保存前后快照、人工决策和日期类型调整，可查看历史差异。
- 软超载、每日任务数量、长任务数量、同科目占比、记忆任务分散和连续高负载规则。
- 游客演示与不同账号数据空间隔离；登录后自动恢复和保存，退出时隐私遮罩。

## 部分实现

- 手机端长按任务会打开“移动任务”快捷抽屉并提供日期选择；受浏览器触摸拖放兼容性限制，没有强制模拟桌面式跨格拖动。
- 缓冲日已参与负载评分与建议，但仍不是独立日期类型。
- 任务拆分已有数据字段、部分完成和剩余时间，尚不会自动生成多个独立可勾选片段。
- 前后对比已覆盖所有发生变化的日期，但不是两个完整月历并排展示。

## 暂未实现

- 浏览器系统通知。
- 完整每日复盘问卷。
- DeepSeek API（继续只保留辅助扩展位置）。
- 多人协作与共享计划。
- 自动拆分任务为独立片段。

## 验证

- 严格 TypeScript 静态检查通过。
- 检查了月历三类点击事件隔离、“+N 项”拖放入口、重排双栏差异、日期微调和历史快照数据链路。
- Supabase 表结构和环境变量未变化。
- 当前执行环境的内部 npm 镜像缺少 `@supabase/supabase-js`，因此未完成真实 Vite 生产构建；严格 TypeScript 检查已通过，Vercel 部署时会从正常 npm 源安装并构建。
