# V0.6.0 实现状态

## 已实现：真实执行驱动的滚动重排

- 过去日期完全冻结；已完成历史不会被重新生成覆盖。
- 今天半冻结：实际计时、补录时间、推定用时和已完成同类任务均参与验算。
- 全面重排默认从明天开始；只有用户明确填写今日额外可用时间时，今天才可继续承担剩余任务。
- 正在计时任务完全冻结；部分完成任务默认保留，移动时只计算剩余工作量并保留原进度和实际时间。
- 手动安排长期保护，锁定任务绝不移动；72 小时防拉回继续作为附加保护。
- 局部修复先检查问题日前后设定范围，完整验证来源日与目标日；局部无解才扩大搜索。
- 无合法方案时保留为待决定，并提供一次性放宽上限、增加容量、使用缓冲日或调整目标日期等选择。
- 最少改动、平衡执行、目标优先、休息缓冲四类候选方案。
- 最少改动方案只修复真实问题；平衡方案保留约 15% 余量；目标优先可接近满载但不静默突破硬限制。
- 方案记录改动日期、任务移动、原日期保留率、平均/最大负载变化和任务包整体顺延数量。

## 已实现：统一每日上限

- 文言文学习/理解：每天 4 次。
- 文言文默写：每天 1 篇。
- 正式背诵训练：每天 1 次。
- 化学预习课：每天 1 节。
- 数学整套试卷：每天 1 套。
- 90 分钟及以上长任务：常规日 1 个、学习日 2 个。
- 高强度任务：每天 2 个。
- 任务组自身的 `dailyMax` 仍可设置，并与上述活动类型限制同时生效。
- 所有上限默认按硬限制执行；冲突预览可按指定日期和限制仅本次放宽。
- 已完成、正在进行、部分完成、未完成和准备移入的任务共同参与当日数量验算。

## 已实现：活动日与缓冲日

- 日期详情支持默认容量、降低容量、完全休息三种状态。
- 可填写可用分钟、活动原因和“保持后续安排 / 保护目标 / 均匀分散”偏好。
- 手动缓冲日默认受保护，全面重排、局部修复和目标优先均不会擅自新增任务。
- 重排预览可只为本次开放某个受保护缓冲日，不会永久取消保护。
- 完全休息时优先保持原任务包并整体顺延；部分可学习时只移走必要任务，优先保留重要、锁定或适合活动日完成的任务。
- 休息缓冲方案可自动生成未受保护的轻量缓冲位置，并避免安排高强度任务。
- 自动新增缓冲日前会验算剩余工作量和目标利用率安全余量；计划本身接近满载时只保留已有缓冲，不为凑数量制造未安排任务。
- 计划扰动通过新增任务数、负载变化预算、原日期距离和同一原日任务的共同位移进行控制。

## 已实现：交互安全与游客体验

- 已完成任务旁不再显示容易误触的刷新图标。
- “重新打开任务”移动到更多菜单，操作前二次确认；进度重置，但实际学习时间保留。
- 日历单项与批量移动会检查全部活动上限、任务组上限、长任务、高强度、容量和目标/截止风险。
- 游客默认演示为约 29 天的完整规模计划，覆盖套卷、预习课、文言文、默写、背诵、重复任务、长任务、缓冲日、部分完成和计时数据。
- 游客可恢复完整演示，也可一键从空白计划开始。
- 原有月历 +N 快捷面板、前后双栏比较、逐项微调、沉浸式计时、统计中心、云同步和隐私隔离继续保留。

## 仍有边界

- 当前重排仍是确定性规则引擎，不使用 DeepSeek；自然语言排期顾问尚未接入。
- 单个任务仍以一个 Assignment 为基本排期单位，尚未自动拆成多个独立跨日片段。
- 浏览器系统通知、完整每日复盘问卷和多人协作尚未实现。
- 旧数据如果缺少完成时间与时间记录，只能使用计划日期和预计时长推定实际负载，并在统计意义上保持“不伪造精确值”。
- 用户在预览中手动把任务拖到违规日期时，复杂约束会在延迟复检中提示；尚未为预览内每次拖动单独弹出完整限制确认。

## 验证

- `tsconfig.check.json` 严格 TypeScript 静态检查通过。
- 使用独立运行测试覆盖：今日已学满后的全面重排、已完成文言文计入当日上限、局部修复不向今天错误移入、完全休息任务包整体顺延、部分活动日只移出必要任务、游客大型演示数据生成。
- Supabase 表结构和环境变量未变化。
- 当前环境未安装完整 npm 依赖，因此不宣称完成真实 Vite 生产构建；Vercel 会从正常 npm 源安装依赖并构建。
