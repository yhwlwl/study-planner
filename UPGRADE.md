# 从 V0.3.0 升级到 V0.4.0

1. 建议先在网页“设置”中导出一份 JSON 备份。
2. 将补丁压缩包内的文件上传到 GitHub 仓库根目录，覆盖同名文件。
3. 本次新增文件：
   - `src/components/FocusTimerPage.tsx`
4. 不需要删除任何旧文件。
5. Vercel 会在上传后自动重新部署。
6. Supabase 表结构和环境变量均未变化，不需要重新执行 SQL。

计时状态沿用原有数据结构，旧版数据可直接兼容。
