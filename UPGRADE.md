# 从 V0.2.0 升级到 V0.2.1

1. 建议先在网页设置中导出一份 JSON 备份。
2. 将本版本 `study-planner/` 内全部内容上传并覆盖原 GitHub 仓库。
3. 本次没有需要额外删除的旧文件。
4. Vercel 会自动重新构建部署。
5. Supabase 表结构与环境变量都没有变化，不需要重新执行 SQL。

本版本主要修复重排方案选择状态，并增加整体顺延预览。
