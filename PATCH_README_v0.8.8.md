# Study Planner v0.8.8 覆盖补丁说明

本补丁以完整 v0.8.7 源码为基线，不适用于更早版本直接覆盖。

## 主要变化

- 复盘、计划调整入口和方案预览增加独立桌面弹窗规格。
- 复盘桌面摘要改为三列两行，手机保留两列。
- 计划调整桌面改为主次双栏，手机自动回落单列。
- 今日容量选择改为 2×2 卡片。
- 方案预览桌面四列摘要、手机两列摘要。
- 统一目标、统计、设置、任务组、日期约束和表单视觉层级。
- 新增 15 项 UI 布局审计。

## 覆盖方式

1. 备份 v0.8.7 项目与用户数据。
2. 将补丁 ZIP 解压到 v0.8.7 项目根目录并允许覆盖。
3. 执行：

```bash
npm install
npm run typecheck
npm run test:ui
npm run validate:v08
npm run build
```
